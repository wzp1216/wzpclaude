# linuxcmd.md — Linux 常用命令速查手册

> **快速参考卡片** — 覆盖日常使用的 Linux 常用命令，按类别分章，按需跳读；vim 单独成章。细节可随时问 AI。

---

## 目录

1. [文件与目录命令](#1-文件与目录命令)
2. [磁盘命令](#2-磁盘命令)
3. [进程管理命令](#3-进程管理命令)
4. [网络命令](#4-网络命令)
5. [其他常用命令](#5-其他常用命令)
6. [vim 编辑器](#6-vim-编辑器)

---

## 1. 文件与目录命令

### 1.1 查看目录与位置

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `ls` | 列出目录内容 | `ls -la` 全部文件（含隐藏）+详情；`ls -lh` 人性化显示大小 |
| `pwd` | 显示当前所在目录 | `pwd` |
| `tree` | 树形显示目录结构 | `tree`；`tree -L 2` 只显示两层 |

### 1.2 切换目录

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `cd` | 切换目录 | `cd ~` 回主目录；`cd -` 回上一个目录；`cd ..` 上一级；`cd /` 根目录 |

### 1.3 创建与删除

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `mkdir` | 创建目录 | `mkdir 项目`；`mkdir -p a/b/c` 级联创建 |
| `touch` | 创建空文件 / 更新时间戳 | `touch 新建.txt` |
| `rmdir` | 删除空目录 | `rmdir 空目录` |
| `rm` | 删除文件或目录 | `rm 文件`；`rm -r 目录` 递归删除；`rm -f` 强制不提示 |

> ⚠️ **危险命令**：`rm -rf` 删除后不可恢复。执行前务必确认路径（`rm -rf /`、`rm -rf ~` 会毁掉整个系统或主目录）。删除重要文件前先备份。

### 1.4 复制与移动

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `cp` | 复制文件/目录 | `cp a.txt b.txt`；`cp -r 目录A 目录B` 复制目录 |
| `mv` | 移动 / 重命名 | `mv a.txt 子目录/` 移动；`mv a.txt b.txt` 重命名 |

### 1.5 链接

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `ln -s` | 创建软链接（快捷方式） | `ln -s /真实/路径 链接名` |

### 1.6 查找

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `find` | 按条件查找文件 | 见下方常用组合 |
| `locate` | 从索引库快速查找 | `locate linuxcmd.md`（索引由系统定时更新） |

`find` 常用组合：

```bash
find . -name "*.py"                    # 按文件名（支持通配符）
find . -iname "*.PY"                   # 忽略大小写查找
find ~ -type d -name "venv*"           # 按类型：f 文件 / d 目录 / l 链接
find . -size +100M                     # 按大小：+大于 / -小于
find . -mtime -7                       # 7 天内修改过的文件（+7 为 7 天前）
find . -maxdepth 2 -name "*.md"        # 限制搜索深度（只搜两层）
find . -name "*.log" -exec rm {} \;    # 对找到的文件执行命令（{} 代表文件）
find . -name "*.log" -delete           # 直接删除，比 -exec rm 更简单
```

> 文件名含空格时用 `-exec` 或 `-delete` 比管道 `xargs` 更安全。

### 1.7 查看文件内容

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `cat` | 一次性输出全文 | `cat 文件.txt`；`cat a.txt b.txt` 合并输出 |
| `less` | 分页查看（可上下翻） | `less 大文件.log`（`/关键词` 搜索，`q` 退出） |
| `head` | 看开头 | `head -n 20 文件` 前 20 行 |
| `tail` | 看结尾 / 实时跟踪 | `tail -n 50 文件`；`tail -f 运行日志.log` 实时滚动（Ctrl+C 退出） |

### 1.8 内容搜索与统计

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `grep` | 搜索文件内容 | `grep "关键词" 文件`；`grep -rn "关键词" 目录` 递归+行号；`grep -i` 忽略大小写 |
| `wc` | 统计行/词/字符数 | `wc -l 文件` 行数；`ls | wc -l` 文件个数 |

### 1.9 权限管理

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `chmod` | 修改文件权限 | `chmod +x 脚本.sh` 加执行权限；`chmod 755 脚本.sh` 数字方式 |
| `chown` | 修改文件属主 | `sudo chown wzp:wzp 文件`（普通用户一般用不上） |

数字权限速记：`r=4 读 / w=2 写 / x=1 执行`，如 `755` = 属主读写执行 + 组读执行 + 其他读执行。

### 1.10 打包与压缩

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `tar` | 打包/解包（最常用） | `tar -czvf 打包.tar.gz 目录` 打包压缩；`tar -xzvf 打包.tar.gz -C 目标目录` 解压到指定位置 |
| `zip` / `unzip` | zip 格式压缩/解压 | `zip -r 压缩包.zip 目录`；`unzip 压缩包.zip` |
| `gzip` / `gunzip` | gz 单文件压缩/解压 | `gzip 文件`；`gunzip 文件.gz` |

记忆法：`-c 创建 / -x 解压 / -z gzip / -v 显示过程 / -f 指定文件`，打包 czvf、解包 xzvf。

---

## 2. 磁盘命令

### 2.1 查看空间

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `df -h` | 各分区使用情况（人性化单位） | `df -h`；`df -h /home` 只看某挂载点 |
| `du -sh` | 目录/文件占用大小 | `du -sh 目录`；`du -sh *` 当前目录各子项大小 |

磁盘满了的排查套路：`df -h` 找哪个分区满了 → `du -sh /*` 逐层往下找占用大户。

### 2.2 设备与挂载

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `lsblk` | 列出所有块设备（磁盘/U 盘） | `lsblk`（U 盘常见设备名 `/dev/sdb1`） |
| `mount` | 挂载设备 | `sudo mount /dev/sdb1 /mnt` |
| `umount` | 卸载设备 | `sudo umount /mnt`（先退出该目录再卸载） |
| `fdisk -l` | 查看分区表 | `sudo fdisk -l`（只看不操作，分区编辑需谨慎） |
| `fsck` | 检查修复文件系统 | 需先卸载再使用，日常很少用到 |

> Ubuntu 桌面插入 U 盘通常自动挂载到 `/media/用户名/卷标`，一般无需手动 mount。

---

## 3. 进程管理命令

### 3.1 查看进程

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `ps` | 查看进程快照 | `ps aux` 所有进程详情；`ps aux | grep 关键词` 找特定进程 |
| `top` | 实时监控进程 | `top`（`P` 按 CPU 排、`M` 按内存排、`q` 退出） |
| `htop` | top 的彩色加强版 | `htop`（需安装：`sudo apt install htop`） |

### 3.2 结束进程

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `kill` | 按 PID 结束进程 | `kill 1234` 正常终止；`kill -9 1234` 强制杀死（最后手段） |
| `pkill` | 按名字结束进程 | `pkill python3`；`pkill -f "脚本名.py"` 匹配完整命令行 |
| `killall` | 按进程名结束 | `killall 进程名` |

套路：`pgrep -f "关键词"` 找到 PID → `kill PID`；还不行再 `kill -9`。

### 3.3 前后台运行

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `命令 &` | 后台运行 | `python3 脚本.py &` |
| `jobs` | 查看本终端后台任务 | `jobs` |
| `fg` / `bg` | 调回前台 / 转后台继续 | `fg %1`；`bg %1` |
| `Ctrl+Z` | 暂停当前前台任务 | 暂停后可用 `bg` 转后台或 `fg` 调回 |
| `nohup` | 关终端也不中断的运行 | `nohup python3 脚本.py > 输出.log 2>&1 &` |

> 桌面用户长期运行的服务推荐 **systemd 用户服务**（见 3.4），比 nohup 更可靠（开机自启、自动重启、日志管理）。

### 3.4 systemd 服务管理

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `systemctl` | 管理系统级服务 | `sudo systemctl status 服务名`；`sudo systemctl restart 服务名` |
| `systemctl --user` | 管理用户级服务（无需 sudo） | `systemctl --user status 服务名`；支持 start / stop / restart / enable |
| `systemctl list-units` | 列出已加载服务 | `systemctl --user list-units --type=service` |

示例：

```bash
# 用户服务日常操作
systemctl --user status check-system.service    # 查看状态
systemctl --user restart check-system.service   # 重启
systemctl --user enable check-system.service    # 开机自启
journalctl --user -u check-system.service -f    # 实时查看该服务日志
```

### 3.5 定时任务

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `crontab -e` | 编辑定时任务 | 进入编辑器，每行一个任务 |
| `crontab -l` | 查看已有定时任务 | `crontab -l` |

Cron 5 字段格式（分 时 日 月 星期）：

```
30 15 * * *   命令            → 每天 15:30 执行
0 9 * * 1-5   命令            → 工作日 9:00 执行
*/10 * * * *  命令            → 每 10 分钟执行
```

---

## 4. 网络命令

### 4.1 查看网络状态

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `ip addr` | 查看本机 IP 地址 | `ip addr`（`ip a` 简写） |
| `ip route` | 查看路由/网关 | `ip route` |
| `ss -tlnp` | 查看监听中的端口 | `ss -tlnp`（t=tcp、l=监听、n=数字、p=进程名） |
| `ping` | 测试网络连通性 | `ping -c 4 域名`（-c 限定次数，否则一直跑） |

> 传统命令 `ifconfig`、`netstat` 已逐步被 `ip`、`ss` 取代，新版 Ubuntu 需装 net-tools 才有。

### 4.2 下载与请求

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `curl` | 发 HTTP 请求/下载 | `curl -O 下载地址` 按文件名下载；`curl -o 新名 地址` 指定文件名；`curl -s -o /dev/null -w "%{http_code}" 地址` 只看状态码 |
| `wget` | 下载文件（支持断点续传） | `wget 下载地址`；`wget -c` 断点续传 |

### 4.3 远程连接与传输

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `ssh` | 远程登录 | `ssh 用户名@服务器IP`；`ssh -p 端口 用户名@IP` |
| `scp` | 远程拷贝文件 | `scp 本地文件 用户名@IP:远程路径`；反方向把冒号放前面；目录加 `-r` |
| `rsync` | 增量同步（更高效） | `rsync -avz 本地目录/ 用户名@IP:远程路径/`（`--delete` 同步删除） |

免密登录：`ssh-keygen` 生成密钥 → `ssh-copy-id 用户名@IP` 把公钥送到服务器，之后 ssh/scp/rsync 不再要密码。

### 4.4 域名解析

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `nslookup` | 查域名对应 IP | `nslookup www.example.com` |
| `dig` | 更详细的 DNS 查询 | `dig www.example.com`；`dig +short` 只看结果 |

---

## 5. 其他常用命令

### 5.1 管道与重定向（先掌握这个）

把命令的输出当作输入传给下一个命令，或写入文件：

```
命令A | 命令B           → A 的输出作为 B 的输入（管道）
命令 > 文件             → 输出写入文件（覆盖）
命令 >> 文件            → 输出追加到文件末尾
命令 < 文件             → 从文件读取输入
命令 2>&1               → 错误输出合并到标准输出
命令A && 命令B          → A 成功才执行 B
命令A || 命令B          → A 失败才执行 B
```

实际例子：

```bash
ls -l | grep "\.py$"                    # 列出所有 .py 文件
ps aux | grep python3 | grep -v grep    # 找 python3 进程（排除 grep 自身）
tail -f 日志.log | grep --line-buffered "ERROR"   # 实时只看报错行
python3 脚本.py > 输出.log 2>&1          # 运行并把所有输出存日志
sudo apt update && sudo apt upgrade -y  # 更新成功才升级
```

### 5.2 终端辅助

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `echo` | 输出文本/变量 | `echo $PATH`；`echo "文本" > 文件` |
| `history` | 查看命令历史 | `history`；`history | grep 关键词`；`!编号` 重跑某条 |
| `alias` | 自定义命令别名 | `alias ll='ls -lh'`（临时）；写入 `~/.bashrc` 永久生效 |
| `clear` | 清屏 | `clear`（或 `Ctrl+L`） |
| `watch` | 定时重复执行命令 | `watch -n 2 df -h` 每 2 秒看一次磁盘 |
| `xargs` | 把管道输入转成命令参数 | `find . -name "*.log" | xargs rm` 批量删除 |

### 5.3 系统信息

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `uname -a` | 系统与内核信息 | `uname -a` |
| `date` | 显示/格式化日期时间 | `date`；`date "+%Y-%m-%d %H:%M:%S"` |
| `uptime` | 开机时长与负载 | `uptime` |
| `who` / `w` | 谁在线 / 在做什么 | `who`；`w` |

### 5.4 软件安装（Ubuntu）

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `sudo` | 以管理员身份执行 | `sudo 命令` |
| `apt update` | 刷新软件源索引 | `sudo apt update` |
| `apt upgrade` | 升级已安装软件 | `sudo apt upgrade -y` |
| `apt install` | 安装软件 | `sudo apt install htop`；`-y` 自动确认 |
| `apt remove` | 卸载软件 | `sudo apt remove 包名` |
| `apt search` | 搜索软件包 | `apt search 关键词` |

### 5.5 帮助与日常

| 命令 | 功能 | 常用示例 |
|------|------|----------|
| `man` | 查看命令手册 | `man tar`（`q` 退出）；`命令 --help` 更快捷 |
| `which` / `whereis` | 查找命令所在位置 | `which python3`；`whereis python3` |
| `journalctl` | 查看系统/服务日志 | `journalctl --user -u 服务名 -f` |
| `notify-send` | 发送桌面通知 | `notify-send "标题" "内容"` |
| `shutdown` / `reboot` | 关机 / 重启 | `shutdown -h now`；`reboot` |

---

## 6. vim 编辑器

vim 是 Linux 自带的神级文本编辑器，初学只需记住两件事：**先按 `i` 进入插入模式才能打字；打完按 `Esc`，输入 `:wq` 保存退出**。

### 6.1 三种模式

```
┌──────────┐   按 i / a / o    ┌──────────┐
│  普通模式 │ ───────────────→ │  插入模式 │
│ （移动、  │ ←─────────────── │ （打字）   │
│  删除、   │     按 Esc       └──────────┘
│  复制）   │
└────┬─────┘
     │ 按 : 或 /
     ↓
┌──────────┐
│ 命令行模式 │  在底部输入命令：保存、退出、查找、替换
└──────────┘
```

- 刚打开 vim 时处于**普通模式**，所有按键都是命令
- 按 `i`（光标前插入）/ `a`（光标后）/ `o`（下一行）进入插入模式
- 任何时候按 `Esc` 退回普通模式
- 普通模式下按 `:` 进入命令行模式

### 6.2 打开与退出

| 操作 | 命令 | 说明 |
|------|------|------|
| 打开文件 | `vim 文件` | 文件不存在则新建 |
| 保存 | `:w` | 写入文件 |
| 保存并退出 | `:wq` | 最常用 |
| 不保存强制退出 | `:q!` | 放弃所有修改 |
| 普通模式下保存退出 | `ZZ` | 等价 `:wq` |
| 不保存退出（没改动时） | `:q` | 有改动会拒绝，需 `:q!` |

### 6.3 移动光标（普通模式）

| 按键 | 动作 | 按键 | 动作 |
|------|------|------|------|
| `h` / `l` | 左 / 右一字符 | `j` / `k` | 下 / 上一行 |
| `w` / `b` | 下一个 / 上一个单词开头 | `e` | 单词结尾 |
| `0` / `$` | 行首 / 行尾 | `gg` / `G` | 文件开头 / 结尾 |
| `Ctrl+f` / `Ctrl+b` | 下翻 / 上翻一屏 | `Ctrl+d` / `Ctrl+u` | 下翻 / 上翻半屏 |
| `数字G` | 跳到第 N 行（如 `20G`） | `数字+方向` | 移动 N 次（如 `5j`） |

> 熟记 `h j k l` 后可以脱离方向键，效率大增。

### 6.4 编辑（普通模式）

| 按键 | 动作 |
|------|------|
| `x` | 删除光标处字符 |
| `dd` | 删除（剪切）整行；`5dd` 删 5 行 |
| `yy` | 复制整行；`5yy` 复制 5 行 |
| `p` | 粘贴到光标后；`P` 粘贴到光标前 |
| `u` | 撤销；`Ctrl+r` 重做 |
| `r` | 替换光标处一个字符（如 `rX` 换成 X） |
| `cw` | 删除光标到单词尾并进入插入模式 |
| `dw` | 删除一个单词 |
| `.` | 重复上一次操作（神器） |

### 6.5 查找与替换

| 操作 | 命令 | 说明 |
|------|------|------|
| 查找 | `/关键词` 回车 | `n` 下一个匹配，`N` 上一个 |
| 全文替换 | `:%s/旧/新/g` | `g`=全部替换，不加只换每行第一个 |
| 确认替换 | `:%s/旧/新/gc` | 每处询问 y/n |
| 指定范围 | `:10,20s/旧/新/g` | 只替换第 10-20 行 |

### 6.6 实用小技巧

| 操作 | 命令 |
|------|------|
| 显示行号 | `:set nu`（取消 `:set nonu`） |
| 跳到指定行 | `:20` |
| 垂直分屏 | `:vsplit 文件名`；窗口切换 `Ctrl+w` 再按方向键 |
| 水平分屏 | `:split 文件名` |
| 关闭当前窗口 | `:q` |

常用配置写入 `~/.vimrc`（如无则创建）：

```vim
set number        " 默认显示行号
set tabstop=4     " Tab 宽度为 4 空格
set shiftwidth=4  " 缩进宽度为 4 空格
set expandtab     " Tab 自动转空格（Python 必须）
```

### 6.7 多文件切换

vim 可同时打开多个文件（每个文件叫一个缓冲区 buffer），在文件间来回切换：

| 操作 | 命令 | 说明 |
|------|------|------|
| 同时打开多个文件 | `vim 文件1 文件2` | 之后用 `:bn` / `:bp` / `Ctrl+^` 切换 |
| 打开另一个文件 | `:e 文件路径` | 当前窗口切换到该文件 |
| 列出已打开的文件 | `:ls`（或 `:buffers`） | 每个文件有编号 |
| 切到下一个 / 上一个 | `:bn` / `:bp` | buffer next / previous |
| 按编号切换 | `:b 编号` | 如 `:b 2` 切到 2 号文件 |
| 按文件名切换 | `:b 文件名` | 支持 Tab 补全 |
| 两个文件间来回切换 | `Ctrl+^` | 在当前文件与上一个文件间切换（最常用） |
| 关闭当前文件 | `:bd` | 关掉缓冲区，不退出 vim |
| 标签页打开文件 | `:tabnew 文件` | 标签页间用 `gt` / `gT` 切换 |

```bash
vim a.py b.py c.py     # 一次打开三个文件
:ls                    # 列出所有缓冲区及编号
:b 2                   # 切到 2 号文件
:bn                    # 下一个文件
Ctrl+^                 # 在最近两个文件间来回切换
:bd                    # 关掉当前文件（不退出 vim）
```

> 标签页（`:tabnew`）整屏切换文件；分屏（`:vsplit`/`:split`）同一屏幕内并排看两个文件，各有用处。

### 6.8 vim 速查表

| 想做什么 | 按键 |
|----------|------|
| 开始打字 | `i`（Esc 退出插入） |
| 保存退出 | `Esc` → `:wq` |
| 放弃修改退出 | `Esc` → `:q!` |
| 删除一行 | `dd`，粘贴回来用 `p` |
| 复制一行 | `yy`，粘贴用 `p` |
| 撤销 | `u`，反撤销 `Ctrl+r` |
| 跳到文件开头/结尾 | `gg` / `G` |
| 查找关键词 | `/关键词`，下一个 `n` |
| 全文替换 | `:%s/旧/新/g` |
| 切换文件 | `Ctrl+^`（或 `:bn` / `:bp`） |

---

*最后更新：2026-08-14*
*生成工具：Claude Code*
